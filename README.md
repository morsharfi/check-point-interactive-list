# 🌟 Interactive List Component

A reusable **React + TypeScript** component for managing a dynamic list of items.
Includes support for adding, editing, removing, validating and displaying items, with Storybook documentation.

---

## 🚀 Features

* Add items (button or Enter key)
* Edit items (inline editing)
* Remove items (hover actions)
* Built-in empty state
* Read-only mode
* Error state styling
* Custom validation
* Regex validation support
* Loading state with CSS-only spinner
* Keyboard accessible

---

## 📘 Storybook

The component is fully documented with Storybook, including examples for:

* Basic usage
* Empty state
* Loading state
* Validation flows
* Read-only mode

Run Storybook:

```bash
npm install
npm run storybook
```

---

## ▶️ Running Locally

```bash
npm install
npm run dev
```

---

## 📦 Project Structure (High-Level)

```
src/
 ├── components/
 ├── hooks/
 ├── styles/
 ├── stories/
 ├── App.tsx
 └── main.tsx
```

---

## 🛠 Tech Stack

* React
* TypeScript
* Storybook
* Vite
* ESLint + Prettier
* Pure CSS animations
